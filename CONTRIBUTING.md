# Contributing

Create a focused branch, keep changes backward compatible, and never commit `.env` files or credentials. Before opening a pull request, run `npm run lint`, both workspace test commands, and both production builds. Add tests for authorization and validation changes. Report security issues privately to the maintainers rather than in a public issue.
